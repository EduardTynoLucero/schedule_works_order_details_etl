# ========== API ==========
API_BASE_URL=https://www.vevidental.com/dlabs/api/services/v1
API_TOKEN=

# ========== BD ==========
DB_HOST=
DB_USER=
DB_PASS=
DB_NAME=
DB_PORT=3306

# ========== POOL DE BD (opcional) ==========
# Conexiones maximas por proceso (el ETL trabaja en secuencia; 5 sobra)
DB_POOL_LIMIT=5
# Conexiones ociosas que se mantienen abiertas (las demas se cierran a los 60s)
DB_POOL_MAX_IDLE=2
# Prepared statements guardados por conexion (mysql2 trae 16000 por defecto)
DB_MAX_PREPARED_STATEMENTS=50

TZ=America/Guatemala

# ========== DETALLE DE ORDENES ==========
# 0 = loop continuo (corre, espera 1 min, vuelve a correr) | 1 = una sola corrida
ETL_RUN_ONCE=0

WORK_DETAILS_BATCH_SIZE=100
WORK_DETAILS_CONCURRENCY=2
# 0 = sin limite
WORK_DETAILS_LIMIT=0
WORK_DETAILS_START_AFTER_WORK_ID=0
# 1 = solo works sin registro en external_work_details
WORK_DETAILS_ONLY_MISSING=1
WORK_DETAILS_BATCH_DELAY_MS=250
